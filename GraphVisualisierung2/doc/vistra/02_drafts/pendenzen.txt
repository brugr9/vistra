/*************** Done ****/
Maven:
- pom.xml ohne scm-einträge, damit build funzt

Utils partially moved:
- gravis.util (allgemein)
- gravis.core.util (core spezifisch)
- gravis.core.graph.util (graph spezifisch)

Application:
- GravisStartup.java wird Startup.java

GravisColor:
- z.T. umbenannt, z.T. neu: light__<colour>, dark_<colour>
- RGB Werte gemäss Skript ADT

Algorithms:
- AlgorithmEvil gelöscht
- AlgorithmFactoryTest: vorerst auskomentiert, damit build funzt
- AlgorithmDefault gelöscht
- id und dessen getter/setter gelöscht

MVC:
- Control: verbessert
- Model: angepasst
- View:
-- generell Grösse der Panelen fixiert (Startup, ApplicationFactory, GuiFactory,
IView, ViewFull, ViewMinimal, XYPanel)
-- ViewFull und ViewMinimal: resizable(false), d.h. fixe Grösse
-- ApplicationFactory: createXYPanel gelöscht, da Zirkelbezug
--> Objekte werden direkt im Panel-Konstruktor instanziert (so auch der VisualizationViewer im VisualizationPanel)
-- GuiFactory: createLayout() @deprecated (wird nur noch von gravis.old gebraucht)
-- GravisVisualizationViewer @deprecated und nach gravis.old verschoben (wird nur noch von gravis.old gebraucht)
select layout (circle of tree)

Core:
- Core-Konstruktor instanziert selber seine Parameter (CoreFactory: Methoden gelöscht)

EdgeType:
- IAlgorithm: Enum GraphType gelöscht, es wird Enum EdgeType verwendet

IParameterManager, AbstractParameterManager:
- Parameter-Listen werden in AlgorithmManager und GraphManager gehalten

Traversal:
- Traversal (inkl. ITraversal, TraversalChangeListener und TraversalChangeEvent) gelöscht
- Parameter Graph wird im Model gehalten (innerhalb des Layouts)
- Parameter Algorithm muss nicht gehalten werden

IteratorManager:
- IIteratorManager und IteratorManager gelöscht
- PlayerListener im Control ist quasi ein IteratorManager

Graph und Algorithm Templates:
- src/main/resources/META-INF/templates entfällt und wird ersetzt durch ...
gravis.core.graph.templates
gravis.core.algorithm.templates

Graph und Algorithm Workbench:
- src/main/resources/META-INF/workbench entfällt und wird ersetzt durch ...
<userHome>/gravis/graph
<userHome>/gravis/algorithm

/*************** In progress ****/

AlgorithmFactroy, GraphFactroy:
- Konstruktoren I/O


/*************** TODO ****/

Buttons:
-image

select layout (circle of tree)
- Class Cast Exception für Tree

/************************************************************************/
Source:
- package ch.bfh.bti7301.hs2013.gravis vorübergehend umbenennen nach gravis?

Package command:
- rename to step

Package Common:
- irgendwann entscheiden: wohin mit den Interfaces für den Actor Algorithm-Developer (IAlgorithm etc.)
- AlgorithmTemplate

GravisEdge, GravisVertex:
- clone: deep clone?

GravisGraphEvent ...

